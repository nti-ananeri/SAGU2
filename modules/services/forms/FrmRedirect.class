<?php

class FrmRedirect extends MForm
{

    /**
     * Class constructor
     **/
    function __construct($data)
    {
        parent::__construct(_M('Redirect', 'services'));
        $this->setHelp(get_class($this), MIOLO::getCurrentModule(), MIOLO::getCurrentAction());
        $this->eventHandler();
    }

    /**
     * Default method to define fields
     **/
    public function createFields()
    {
        $MIOLO    = MIOLO::getInstance();
        $action   = MIOLO::getCurrentAction();
        $function = MIOLO::_REQUEST('function');

        $uid  = 'sagu2';
        $pwd  = 'sagu2';
        $auth = $this->manager->auth;

        if ( $auth->Authenticate($uid, $pwd) )
        {
            $this->page->mGoto($MIOLO->getActionURL('research', 'main:process:avaliation', null, array('formId'=>'6')));
        }

    }

}

?>
