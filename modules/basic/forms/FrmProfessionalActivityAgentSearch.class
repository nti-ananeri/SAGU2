<?php

/**
 *
 * This file handles the connection and actions for basProfessionalActivityAgent table
 *
 * @author Daniel Afonso Heisler [daniel@solis.coop.br] 
 *
 * $version: $Id$
 *
 * \b Maintainers \n
 * Alexandre Heitor Schmidt [alexsmith@solis.coop.br]
 * Daniel Afonso Heisler [daniel@solis.coop.br]
 * Jamiel Spezia [jamiel@solis.coop.br]
 * William Prigol Lopes [william@solis.coop.br]
 * 
 * @since
 * Class created on 09/09/2005
 *
 * \b @organization \n
 * SOLIS - Cooperativa de Soluções Livres \n
 * The Sagu2 development team
 *
 * \b Copyleft \n
 * Copyleft (L) 2005 - SOLIS - Cooperativa de Soluções Livres \n
 *
 * \b License \n
 * Licensed under GPL (for further details read the COPYING file or http://www.gnu.org/copyleft/gpl.html )
 *
 * \b History \n
 * See history in CVS repository: http://sagu.solis.coop.br   
 *
 **/

/**
 * Form to search the basProfessionalActivityAgent table
 */
class FrmProfessionalActivityAgentSearch extends MForm
{
    private $home;
    private $module;

    /**
     * Class constructor
     */
    function __construct($data)
    {
        $this->module = 'basic';
        $module = $this->module;

        $this->home   = $data->home;

        parent::__construct(_M('Search',$module));

        // hide the 'Submit' button
        $this->setShowPostButton(false);

        $this->setHelp(get_class($this), MIOLO::getCurrentModule(), MIOLO::getCurrentAction());

        $this->eventHandler();

    }

    /**
     *  Default method to define fields
     */
    public function createFields()
    {
        $MIOLO  = MIOLO::getInstance();
        $module = MIOLO::getCurrentModule();
        $action = MIOLO::getCurrentAction();

        $toolBar = new MToolBar('toolBar',$MIOLO->getActionURL($module,$action));
//        $toolBar->disableButton('tbBtnSearch');
        $toolBar->disableButton('tbBtnSave');
        $toolBar->disableButton('tbBtnDelete');
//        $toolBar->disableButton('tbBtnPrint');

        //favoritos
        $enabledImage  = $MIOLO->getUI()->GetImageTheme($MIOLO->theme->id, 'bookmark-20x20.png');
        $disabledImage = $MIOLO->getUI()->GetImageTheme($MIOLO->theme->id, 'bookmark-disabled-20x20.png');
        $url           = $MIOLO->getActionURL($module, $this->home, null, array('function' => 'search', 'event' => 'bookmark'));
        $toolBar->addButton('tbBtnBookmark', _M('Add to bookmarks', 'basic'), $url, null, true, $enabledImage, $disabledImage);

        if ( MIOLO::_request('event') == 'bookmark' )
        {
            $MIOLO->getClass('basic', 'access');
            access::insert('Professional activity agent', $this->home, 'professionalActivityAgent-16x16.png', true);
        }
        //fim favoritos

        $fields[] = $toolBar;

//         $professionalActivityAgentIdS = new MTextField('professionalActivityAgentIdS', $this->professionalActivityAgentIdS->value, _M('Code',$module), 10);
//         $professionalActivityAgentIdS->setJsHint(_M('Enter the professional activity agent code',$module));
//         $fields[] = $professionalActivityAgentIdS;
        $fields[]  = new MHiddenField('professionalActivityAgentIdS', $this->getFormValue('professionalActivityAgentIdS', $professionalActivityAgentIdS));

        $professionalActivityAgentDescriptionS = new MTextField('professionalActivityAgentDescriptionS', $this->professionalActivityAgentDescriptionS->value, _M('Description',$module), FIELD_DESCRIPTION_SIZE);
        $professionalActivityAgentDescriptionS->setJsHint(_M('Enter the professional activity agent description',$module));
        $fields[] = $professionalActivityAgentDescriptionS;

        $fields[] = new MButton('btnSearch',_M('Search',$module));

        $this->setFields($fields);

        // get filters values
        $filters->professionalActivityAgentId = $this->professionalActivityAgentIdS->value;
        $filters->description                 = $this->professionalActivityAgentDescriptionS->value;

        unset($this->professionalActivityAgentIdS->value);

        $grdResults = $MIOLO->getUI()->getGrid($module,'GrdProfessionalActivityAgentSearch',$filters);
        $business   = $this->manager->getBusiness($module,'BusProfessionalActivityAgent');
        $data       = $business->searchProfessionalActivityAgent($filters);
        $grdResults->setData($data);

        //###GRID///is_array($data) ? $grdResults->SetScrollHeight(GRID_SCROLL_HEIGHT) : false;

        $this->addField($grdResults);
        $this->setLabelWidth(FIELD_LABEL_SIZE);

        if ( $this->isSubmitted() )
        {
            $this->page->onLoad('document.' . $this->name . '.professionalActivityAgentIdS.focus();');
        }

    }

}

?>
