<?php
/**
 *
 * This file handles the connection and actions for sprLanguage table
 *
 * @author Daniel Afonso Heisler [daniel@solis.coop.br]
 *
 * $version: $Id$
 *
 * \b Maintainers \n
 * Alexandre Heitor Schmidt [alexsmith@solis.coop.br]
 * Daniel Afonso Heisler [daniel@solis.coop.br]
 * Jamiel Spezia [jamiel@solis.coop.br]
 * William Prigol Lopes [william@solis.coop.br]
 * 
 * @since
 * Class created on 22/08/2005
 *
 * \b @organization \n
 * SOLIS - Cooperativa de Soluções Livres \n
 * The Sagu2 development team
 *
 * \b Copyleft \n
 * Copyleft (L) 2005 - SOLIS - Cooperativa de Soluções Livres \n
 *
 * \b License \n
 * Licensed under GPL (for further details read the COPYING file or http://www.gnu.org/copyleft/gpl.html )
 *
 * \b History \n
 * See history in CVS repository: http://sagu.solis.coop.br   
 *
 **/

/**
 * Class to manipulate the sprLanguage table
 **/
class BusinessSelectiveProcessBusLanguage extends Business
{

    /**
     * Make a connection to the database
     * 
     * @param $module (string): The module whose database we should connect. If null, the actual module database is connected.
     *
     * @return (object): A MIOLO Database connection
     **/
    public function getDatabase($module = null)
    {
        $MIOLO = MIOLO::getInstance();
        $MIOLO->getClass('basic','sagu');
        $module = is_null($module) ? 'selectiveProcess' : $module;

        return $MIOLO->getDatabase($module);
    }

    /**
     * List all records from the table handled by the class
     *
     * @param: None
     *
     * @returns (array): Return an array with the entire table
     *
     **/
    public function listLanguage()
    {
        $db  = $this->getDatabase();
        $sql = 'SELECT languageId,
                       description
                  FROM sprLanguage
              ORDER BY description';
        $ret = $db->query($sql);
        //$db->close();
        return $ret;
    }

    /**
     * Return a specific record from the database
     *
     * @param $languageId (integer): Primary key of the record to be retrieved
     *
     * @return (object): Return an object of the type handled by the class
     *
     **/
    public function getLanguage($languageId)
    {
        $sql = 'SELECT languageId,
                       description,
                       shortDescription,
                       userName,
                       TO_CHAR(dateTime,\'' . MASK_TIMESTAMP . '\'),
                       ipAddress
                  FROM sprLanguage
                 WHERE languageId = ?';

        $db     = $this->getDatabase();
        $result = $db->query(SAGU::prepare($sql, $languageId));
        //$db->close();

        $language = new sprLanguage();
        list ( $language->languageId,
               $language->description,
               $language->shortDescription,
               $language->userName,
               $language->dateTime,
               $language->ipAddress ) = $result[0];

        return $language;
    }
    
    /**
     * Do a search on the database table handled by the class
     *
     * @param $filters (object): Search filters
     *
     * @return (array): An array containing the search results
     **/
    public function searchLanguage($filters)
    {
        $sql= 'SELECT languageId,
                      description,
                      shortDescription
                 FROM sprLanguage ';

        if ( strlen((string)$filters->languageId) > 0 )
        {
            $where .= ' AND languageId = ? ';
            $args[] = $filters->languageId;
        }
        if ( strlen((string)$filters->description) > 0 )
        {
            $where .= ' AND TO_ASCII(description) ILIKE TO_ASCII(?) ';
            $args[] = $filters->description . '%';
        }
        if ( strlen((string)$filters->shortDescription) > 0 )
        {
            $where .= ' AND TO_ASCII(shortDescription) ILIKE TO_ASCII(?) ';
            $args[] = $filters->shortDescription . '%';
        }

        unset($result);
        if ( strlen((string)$where) > 0 )
        {
            $sql .= ' WHERE ' . substr($where, 4) . ' ORDER BY description';
            $db = $this->getDatabase();
            $result = $db->query(SAGU::prepare($sql, $args));
            //$db->close();
        }

        return $result;
    }

    /**
     * Insert a new record
     *
     * @param $data (object): An object of the type handled by the class
     *
     * @return True if succed, otherwise False
     *
     **/
    public function insertLanguage($data)
    {
        $sql = 'INSERT INTO sprLanguage
                            (description,
                             shortDescription)
                     VALUES (?,?)';

        $db     = $this->getDatabase();
        $args   = array ( $data->description,
                          $data->shortDescription );
        $result = $db->execute(SAGU::prepare($sql, $args));
        //$db->close();

        return $result;
    }

    /**
     * Update data from a specific record
     *
     * @param $data (object): Data which will replace the old record data
     *
     * @return (boolean): True if succeed, otherwise False
     *
     **/
    public function updateLanguage($data)
    {
        $sql = 'UPDATE sprLanguage
                   SET description      = ?,
                       shortDescription = ?
                 WHERE languageId       = ?';

        $args = array( $data->description,
                       $data->shortDescription,
                       $data->languageId );

        $db   = $this->getDatabase();
        $sqls = SAGU::prepare($sql, $args);

        for ( $i=0; $i<count($sqls); $i++ )
        {
            $result = $db->execute($sqls[$i]);
        }

        //$db->close();

        return $result;
    }

    /**
     * Delete a record
     *
     * @param $languageId (string): Primary key for deletion
     *
     * @return (boolean): True if succeed, otherwise False
     *
     **/
    public function deleteLanguage($languageId)
    {
        $sql = 'DELETE FROM sprLanguage
                      WHERE languageId = ?';

        $db     = $this->getDatabase();
        $result = $db->execute(SAGU::prepare($sql, $languageId));
        //$db->close();

        return $result;
    }

}

?>
