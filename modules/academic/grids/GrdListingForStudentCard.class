<?php

class GrdListingForStudentCard extends MGrid
{
    

    /**
     * Class constructor
     **/
    function __construct($filters)
    {
        $MIOLO  = MIOLO::getInstance();
        $module = MIOLO::getCurrentModule();
        $action = MIOLO::getCurrentAction();

        $valueField = new MTextField('valueField[]',  null, '', '15');
        $selRights  = new MTextLabel('selRights', '', '' );

        $columns = array( new MGridColumn( null ) , /*_M('School',   $module), 'left',  true, null, true, null, false ),*/
						  new MGridColumn( _M('Person',   $module), 'left',  true, null, true, null, false ),
                          new MGridColumn( null ), /*_M('Father',    $module), 'left',  true, null, true, null, false ),*/
                          new MGridColumn( null ), /*_M('Mother',    $module), 'left',  true, null, true, null, false ),*/
                          new MGridColumn( null ), /*_M('Sex',    $module), 'left',  true, null, true, null, false ),*/
                          new MGridColumn( _M('Course',    $module), 'left',  true, null, true, null, false ),
                          new MGridColumn( null ), /*_M('Grade',    $module), 'left',  true, null, true, null, false ),*/
                          new MGridColumn( _M('Serie',    $module), 'left',  true, null, true, null, false ),
                          new MGridColumn( _M('Turn',    $module), 'left',  true, null, true, null, false ),
                          new MGridColumn( null ), /*_M('Class',    $module), 'left',  true, null, true, null, false ),*/
                          new MGridColumn( null ), /*_M('Person id',    $module), 'left',  true, null, true, null, false ),*/
                          new MGridColumn( _M('Birth',    $module), 'left',  true, null, true, null, false ),
                          new MGridColumn( null ), /*_M('Location',    $module), 'left',  true, null, true, null, false ),*/
                          new MGridColumn( null ), /*_M('Neighborhood',    $module), 'left',  true, null, true, null, false ),*/
                          new MGridColumn( null ), /*_M('City',    $module), 'left',  true, null, true, null, false ),*/
                          new MGridColumn( null ) /*_M('Zipcode',    $module), 'left',  true, null, true, null, false ),
                          new MGridColumn( _M('Phone',    $module), 'left',  true, null, true, null, false )*/
                        );
       // this foreach is necessary until the getActionURL function is
       // able to handle an object parameter
       unset($opts);
       if (is_array($filters))
       {
			foreach ( $filters as $k => $v )
			{
				$opts[$k] = $v;
			}
		}

		$url = $MIOLO->getActionURL($module, $action, null, $opts);
		parent::__construct(null, $columns, $url, 0, 0, 'GrdListingForStudentCard');
		//$this->setRowMethod('GrdStudentSemtur', 'myRowMethod');

		$this->addActionSelect();
		/*$btnPrint =  new MButton( 'tbBtnPrint', _M('Print', $module) ); 
		$btnPrint->setReadOnly(true);
		$ctlArray = array($btnPrint);
		// set the controls in the grid
		$this->setControls( $ctlArray );

		$this->eventHandler();*/
    }
}

?>
