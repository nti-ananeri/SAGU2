<?php
/**
 *
 * This file handles the connection and actions for acdElection table
 *
 * @author Alexandre Heitor Schmidt [alexsmith@solis.coop.br]
 *
 * $version: $Id$
 *
 * \b Maintainers \n
 * Alexandre Heitor Schmidt [alexsmith@solis.coop.br]
 * Daniel Afonso Heisler [daniel@solis.coop.br]
 * Jamiel Spezia [jamiel@solis.coop.br]
 * William Prigol Lopes [william@solis.coop.br]
 * 
 * @since
 * Class created on 16/09/2005
 *
 * \b @organization \n
 * SOLIS - Cooperativa de Soluções Livres \n
 * The Sagu2 development team
 *
 * \b Copyleft \n
 * Copyleft (L) 2005 - SOLIS - Cooperativa de Soluções Livres \n
 *
 * \b License \n
 * Licensed under GPL (for further details read the COPYING file or http://www.gnu.org/copyleft/gpl.html )
 *
 * \b History \n
 * See history in CVS repository: http://sagu.solis.coop.br   
 *
 **/

/**
 * Class to manipulate the acdElection table
 **/
class BusinessAcademicBusElection extends Business
{

    /**
     * Make a connection to the database
     * 
     * @param $module (string): The module whose database we should connect. If null, the actual module database is connected.
     *
     * @return (object): A MIOLO Database connection
     **/
    public function getDatabase($module = null)
    {
        $MIOLO = MIOLO::getInstance();
        $MIOLO->getClass('basic','sagu');
        $module = is_null($module) ? 'academic' : $module;

        return $MIOLO->getDatabase($module);
    }

    /**
     * List all records from the table handled by the class
     *
     * @param: None
     *
     * @returns (array): Return an array with the entire table
     *
     **/
    public function listElection()
    {
        return 'Not implemented';
    }
    
    /**
     * Return a specific record from the database
     *
     * @param $electionDate (integer): Primary key of the record to be retrieved
     *
     * @return (object): Return an object of the type handled by the class
     *
     **/
    public function getElection($electionDate)
    {
        $sql = 'SELECT TO_CHAR(A.electionDate,\'' . MASK_DATE . '\'),
                       A.description,
                       A.userName,
                       TO_CHAR(A.dateTime,\'' . MASK_TIMESTAMP . '\'),
                       A.ipAddress
                  FROM acdElection A
                 WHERE A.electionDate = TO_DATE(?,\'' . MASK_DATE . '\')';

        $db     = $this->getDatabase();
        $result = $db->query(SAGU::prepare($sql, $electionDate));
        //$db->close();

        $election = new AcdElection();

        list ( $election->electionDate,
               $election->description,
               $election->userName,
               $election->dateTime,
               $election->ipAddress ) = $result[0];

        return $election; 
    }
    
    /**
     * Do a search on the database table handled by the class
     *
     * @param $filters (object): Search filters
     *
     * @return (array): An array containing the search results
     **/
    public function searchElection($filters)
    {
        $sql = 'SELECT TO_CHAR(A.electionDate,\'' . MASK_DATE . '\'),
                       A.description,
                       A.userName,
                       TO_CHAR(A.dateTime,\'' . MASK_TIMESTAMP . '\'),
                       A.ipAddress
                  FROM acdElection A';

        if ( strlen((string)$filters->electionDate) > 0 )
        {
            $where .= ' AND A.electionDate = TO_DATE(?,\'' . MASK_DATE . '\')';
            $args[] = $filters->electionDate;
        }
        if ( strlen((string)$filters->description) > 0 )
        {
            $where .= ' AND TO_ASCII(A.description) ILIKE TO_ASCII(?)';
            $args[] = $filters->description . '%';
        }

        unset($result);
        if ( strlen((string)$where) > 0 )
        {
            $sql .= ' WHERE ' . substr($where, 4) . '
                   ORDER BY A.description';

            $db = $this->getDatabase();
            $result = $db->query(SAGU::prepare($sql, $args));
            //$db->close();
        }

        return $result;
    }

    /**
     * Insert a new record
     *
     * @param $data (object): An object of the type handled by the class
     *
     * @return True if succed, otherwise False
     *
     **/
    public function insertElection($data)
    {
        $sql = 'INSERT INTO acdElection
                            (electionDate,
                             description)
                     VALUES (TO_DATE(?,\'' . MASK_DATE . '\'),
                             ?)';

        $args = array( 
                       $data->electionDate,
                       $data->description
                     );

        $db     = $this->getDatabase();
        $result = $db->execute(SAGU::prepare($sql, $args));
        //$db->close();

        return $result;
    }

    /**
     * Update data from a specific record
     *
     * @param $data (object): Data which will replace the old record data
     *
     * @return (boolean): True if succeed, otherwise False
     *
     **/
    public function updateElection($data)
    {
        $sql = 'UPDATE acdElection
                   SET description = ?
                 WHERE electionDate = TO_DATE(?,\'' . MASK_DATE . '\')';

        $args = array( 
                       $data->description,
                       $data->electionDate
                     );
                       
        $db   = $this->getDatabase();
        $sqls = SAGU::prepare($sql, $args);

        for ( $i=0; $i<count($sqls); $i++ )
        {
            $result = $db->execute($sqls[$i]);
        }

        //$db->close();

        return $result;
    }

    /**
     * Delete a record
     *
     * @param $electionDate (string): Primary key for deletion
     *
     * @return (boolean): True if succeed, otherwise False
     *
     **/
    public function deleteElection($electionDate)
    {
        $sql = 'DELETE FROM acdElection
                      WHERE electionDate = TO_DATE(?,\'' . MASK_DATE . '\')';

        $db     = $this->getDatabase();
        $result = $db->execute(SAGU::prepare($sql, $electionDate));
        //$db->close();

        return $result;
    }

}

?>
